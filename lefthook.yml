# Lefthook configuration for code quality and security checks
# Reference: https://github.com/evilmartians/lefthook/blob/master/docs/configuration.md

pre-commit:
  parallel: true

pre-push:
  commands:
    packages-audit:
      skip:
        - ref: master
      tags: frontend security
      run: ./scripts/security-audit-fix.sh
